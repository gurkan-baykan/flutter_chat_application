import 'package:flutter/material.dart';

class ProfilOrnek extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("Profil Ornek"),),
        body: Container(
      child: Text("Profil Ornek HOşgeldiniz"),
    ));
  }
}
