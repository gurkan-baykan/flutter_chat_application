import 'package:flutter/material.dart';

class KullaniciOrnek extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("Kullanici Ornek"),),
        body: Container(
      child: Text("Kullanici Ornek"),
    ));
  }
}
